# Frontend

React frontend for Student Course Management System.

## Scripts

- `npm run dev`: starts app on `http://localhost:5173`
- `npm run build`: production build
- `npm test`: test runner

## Notes

- Uses proxy to backend: `http://localhost:5000`
- API calls are made to `/api/*`
